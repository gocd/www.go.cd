---
title: GoCD Events
title_tag_of_header: "Events, Webinars, and Demos | GoCD"
meta_description: "Attend GoCD's upcoming events, demos, webinars and learning sessions to help you get a view of the capabilities of our continuous delivery tool."
meta_keywords: "GoCD, continuous delivery, open source, community, events, learning sessions, webinars, product demos"
---
<div class="inner-banner plugins">
  <div class="container">
    <div class="banner-content">
      <h1 class="caption"><span>Webinars and Events</span></h1>
    </div>
  </div>
</div>
<main>
<div class="demos">
  <div class="container">
    <h3>Product Learning Sessions</h3>
    <p>
      The learning sessions are designed to help you get an overall view of the capabilities of GoCD and answer questions around how to configure your GoCD instance.
    </p>
    <ul class="demo-list">
      <% data.webinars.each do |webinar| %>
      <li>
        <a href="<%= webinar.url %>" title="<%= webinar.date %>">
          <div class="box swing">
            <div class="datebox hover">
              <span class="month"><%= webinar.month %></span>
              <span class="date"><%= webinar.day %></span>
            </div>
          </div>
        </a>
        <div class="details">
          <span class="time"><%= webinar.date %></span>
        </div>
      </li>
      <% end %>
    </ul>
  </div>
</div>
<div class="event">
  <div class="container">
    <h3 class="page-headings">Events and Webinars</h3>
    <ul class="events-list">
      <% data.events.each do |event| %>
      <li>
        <div class="details">
          <a href="<%= event.url %>" class="title"><%= event.title %></a>
          <p><%= event.description %></p>
          <ul class="dates">
            <% event.details.each do |detail| %>
            <li>
              <div class="date"><i class="fa fa-calendar"></i> <%= detail.date %></div>
              <% if detail.location %><div class="location"><i class="fa fa-map-marker"></i> <%= detail.location %></div><% end %>
            </li>
            <% end %>
          </ul>
        </div>
      </li>
      <% end %>
    </ul>
  </div>
</div>
</main>
